{
  "name": "toolsified",
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.js",
    "sync:redesign-preview": "node scripts/sync-redesign-preview.js",
    "build:redesign-preview": "npm --prefix \"./Redesign app\" run build && node scripts/sync-redesign-preview.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
